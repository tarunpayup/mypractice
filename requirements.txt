# Python dependencies for this project.
#
# app.py currently has no third-party imports, so this file is intentionally empty.
# Add packages here as you introduce external dependencies, e.g.:
# requests==2.32.3
